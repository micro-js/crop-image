{
  "name": "@f/crop-image",
  "description": "Crop an image using the browser&#x27;s canvas element",
  "repository": "git://github.com/micro-js/crop-image.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/dataurl-to-blob": "^1.0.0",
    "@f/img-to-canvas": "^1.0.0"
  },
  "devDependencies": {
    "hihat": "^2.6.4",
    "tap-dev-tool": "^1.3.0",
    "tape": "^4.2.2"
  }
}
